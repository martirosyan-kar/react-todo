export default (state = [], action) => {
  switch (action.type) {
    case 'CREATE_CATEGORY':
      return [

      ];
    case 'UPDATE_CATEGORY':
      return [

      ];
    case 'REMOVE_CATEGORY':
      return [

      ];
  }
}